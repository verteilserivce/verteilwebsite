---
---

<div
  id="cookie-banner"
  role="dialog"
  aria-live="polite"
  aria-label="Cookie-Einwilligung"
  class="fixed bottom-0 left-0 right-0 z-50 translate-y-full transition-transform duration-500 ease-out"
>
  <div class="bg-white border-t border-border shadow-2xl">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-6">
        <div class="flex-1 max-w-3xl">
          <div class="flex items-center gap-2 mb-2">
            <svg class="w-5 h-5 text-primary" fill="currentColor" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path d="M257.5 27.6c-.8-5.4-4.9-9.8-10.3-10.6c-22.1-3.1-44.6 .9-64.4 11.4l-74 39.5C89.1 78.4 73.2 94.9 63.4 115L26.7 190.6c-9.8 20.1-13 42.9-9.1 64.9l14.5 82.8c3.9 22.1 14.6 42.3 30.7 57.9l60.3 58.4c16.1 15.6 36.6 25.6 58.7 28.7l83 11c23.2 3.1 46.6-4.6 64.4-21.3l66.7-63.1c17.8-16.8 28.2-39.8 29-64.4l1.2-83.6c.1-5.3-3.7-9.9-8.9-10.8c-30.7-5.9-57.6-23.8-75.1-49.8c-17.5-26-24.2-57.4-18.7-88.1c1.4-7.7 2.7-15.4 4-23.1c.9-5.4-2.8-10.6-8.1-11.6zM208 144a32 32 0 1 1 0 64 32 32 0 1 1 0-64zm-26.7 176c-17.7 0-32-14.3-32-32s14.3-32 32-32s32 14.3 32 32s-14.3 32-32 32zm136-32a32 32 0 1 1 64 0 32 32 0 1 1 -64 0zm104 104a32 32 0 1 1 0-64 32 32 0 1 1 0 64z"/>
            </svg>
            <h3 class="text-base font-bold text-text-primary">Datenschutz-Einstellungen</h3>
          </div>
          <p class="text-sm text-text-secondary leading-relaxed">
            Wir verwenden ausschließlich technisch notwendige Cookies, die für den Betrieb dieser Website erforderlich sind. 
            Externe Dienste (z. B. Analyse-Tools, eingebettete Inhalte) werden erst nach Ihrer ausdrücklichen Einwilligung geladen. 
            Sie können Ihre Entscheidung jederzeit widerrufen.
          </p>
        </div>
        <div class="flex flex-col sm:flex-row gap-3 shrink-0">
          <button
            id="cookie-decline"
            type="button"
            class="px-5 py-2.5 text-sm font-medium text-text-secondary bg-gray-100 hover:bg-gray-200 rounded-full transition-colors duration-200"
          >
            Nur notwendige
          </button>
          <button
            id="cookie-accept"
            type="button"
            class="px-5 py-2.5 text-sm font-medium text-white bg-primary hover:bg-primary-dark rounded-full transition-colors duration-200 shadow-lg shadow-primary/20"
          >
            Alle akzeptieren
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  (function() {
    const STORAGE_KEY = 'vsp_cookie_consent';
    const banner = document.getElementById('cookie-banner');
    const acceptBtn = document.getElementById('cookie-accept');
    const declineBtn = document.getElementById('cookie-decline');

    if (!banner || !acceptBtn || !declineBtn) return;

    const consent = localStorage.getItem(STORAGE_KEY);

    if (!consent) {
      // Show banner with slight delay for better UX
      setTimeout(() => {
        banner.classList.remove('translate-y-full');
      }, 1000);
    }

    function setConsent(accepted: boolean) {
      const value = accepted ? 'accepted' : 'declined';
      localStorage.setItem(STORAGE_KEY, value);
      banner.classList.add('translate-y-full');
      
      // Dispatch custom event for other components to listen
      window.dispatchEvent(new CustomEvent('cookieConsentChanged', { 
        detail: { accepted } 
      }));
    }

    acceptBtn.addEventListener('click', () => setConsent(true));
    declineBtn.addEventListener('click', () => setConsent(false));
  })();
</script>
